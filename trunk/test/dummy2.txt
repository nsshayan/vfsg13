/*
int main(int argc,char *argv[])
{
	printf("\nI am in main function\n");
	char fileLabel[30],b[50],choice[50],d[50];
	printf("\nEnter the file system label\n");
	scanf("%s",fileLabel);
	int blockNo;
	printf("\nEnter the file system size\n");
	scanf("%d",&blockNo);		
	int  flag=1;

	while (flag) {
		printf("\nEnter the valid command or exit to quit\n");		
		//fgets(c,sizeof(c),stdin);
		scanf("%s",choice);
		printf("\n%s\n",choice);

		if(strcasecmp(choice,"create_vfs")==0) {
			create_vfs(fileLabel, blockNo);
			printf("\nFile System is created\n");
			continue;
		}

		else if(strcasecmp(choice,"mount_vfs")==0) {
			root=mount_vfs(fileLabel);
			printf("\nFile System is mounted\n");				
			continue;
		}			
	
		else if(strcasecmp(choice,"makedir")==0) {				
			printf("\nin makedir enter path and subdirectory to create\n");
		//	fgets(b,sizeof(b),stdin);
		//	strcat(d,b);
		//	printf("\n%s\n",d);
			scanf("%s",b);
			strcpy(d,choice);
			strcat(d,b);
			root=makeDir(root,d);
			continue;
		}

		else if(strcasecmp(choice,"print")==0) {
			print(root);
			continue;
		}		

		else if(strcasecmp(choice,"exit")==0)
			flag=0;
		else continue;									
	}

return 0;
}*/




------commands--------------
mountvfs shayan
makedir root vehicle
makedir root transport
makedir root/vehicle 4wheeler
makedir root/vehicle 2wheeler 
makedir root/vehicle/2wheeler hero 
makedir root/transport road
makedir root/transport rail
makedir root/transport air
makedir root/transport/rail platform
makedir root/vehicle/4wheeler benz
makedir root/vehicle 3wheeler
makedir root/vehicle/3wheeler auto
addfile root/vehicle abc.txt addDemo.txt
unmountvfs shayan
listdir root 1 unmountfile.txt
mountvfs shayan
listdir root 1 unmountfile.txt
addfile root/vehicle/3wheeler abc.txt addDemo.txt
addfile root/transport dbc.txt sample.txt
listfile root/transport/dbc.txt sample_out.txt
